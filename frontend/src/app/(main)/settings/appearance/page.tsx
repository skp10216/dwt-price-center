/**
 * 외관 설정 페이지 (공유 라우트)
 *
 * 모든 도메인(admin, settlement, user)에서 접근 가능한 외관 설정.
 * 실제 구현은 admin/settings/appearance와 동일 (재사용).
 */

export { default } from '@/app/(main)/admin/settings/appearance/page';
