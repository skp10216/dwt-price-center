/**
 * 판매가 대시보드 페이지
 * 등록된 모든 모델의 가격을 한눈에 확인할 수 있는 페이지
 */

import { PriceDashboard } from './_components/PriceDashboard';

export default function PriceDashboardPage() {
  return <PriceDashboard />;
}
